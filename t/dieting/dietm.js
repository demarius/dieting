var assert = require('chai').assert

describe('Dieting', function () {
    describe('#init()', function () {
        it('should start a server and listen on a socket', function () {
        })
    })
})
